{
  "name": "vpnmanage-system",
  "private": true,
  "scripts": {
    "lint": "npm --prefix vpnmanage run lint && npm --prefix backend run lint",
    "typecheck": "npm --prefix vpnmanage run typecheck && npm --prefix backend run typecheck",
    "test": "npm --prefix vpnmanage run test && npm --prefix backend run test"
  }
}
